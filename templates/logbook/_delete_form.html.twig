<form method="post" action="{{ path('conduire_delete', {'id': conduire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conduire.id) }}">
	<button class="btn">{% trans %}Delete{% endtrans %}</button>
</form>
